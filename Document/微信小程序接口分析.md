# 微信小程序接口分析报告

## 一、现有接口评估

### ✅ 1. 商品相关接口（完全满足）

#### 已有接口
```java
GET  /api/product/detail/{id}              // 查询商品详情
POST /api/product/list                     // 查询商品列表（分页）
GET  /api/product/list-by-category/{category}  // 根据分类查询商品
```

#### 小程序需求匹配度：100%
- ✅ 商品列表展示（首页）
- ✅ 商品详情查看
- ✅ 商品分类筛选
- ✅ 商品搜索（通过 PageRequest 的 keyword 参数）

#### 数据字段完整性
```java
ProductResponse {
    id, name, category, price, stock, unit, status,
    description, image, batchId, batchNumber,
    farmerId, farmerName, createTime, updateTime
}
```
✅ 字段完整，满足小程序展示需求

---

### ✅ 2. 订单相关接口（基本满足，需补充）

#### 已有接口
```java
POST   /api/order/create                    // 创建订单 ✅
GET    /api/order/detail/{id}               // 查询订单详情 ✅
GET    /api/order/detail-by-number/{orderNumber}  // 根据订单号查询 ✅
POST   /api/order/list-by-customer          // 根据客户ID查询订单列表 ✅
PUT    /api/order/update-status/{id}        // 更新订单状态 ✅
```

#### 小程序需求匹配度：85%

##### ✅ 已满足的功能
1. 创建订单（下单）
2. 查看订单列表（我的订单）
3. 查看订单详情
4. 订单状态更新（确认收货）

##### ⚠️ 需要补充的功能
1. **支付接口**（重要）
   - 缺少：`POST /api/order/pay/{id}` - 发起支付
   - 缺少：`POST /api/order/pay-callback` - 支付回调
   - 需要集成微信支付 API

2. **订单取消接口**
   - 缺少：`PUT /api/order/cancel/{id}` - 取消订单
   - 当前只有删除接口，不适合用户端

3. **订单评价接口**（可选）
   - 缺少：`POST /api/order/review/{id}` - 订单评价

#### OrderRequest 字段分析
```java
OrderRequest {
    customerId,      // ✅ 客户ID
    customerName,    // ✅ 客户名称
    productId,       // ✅ 商品ID
    batchId,         // ✅ 批次ID
    quantity,        // ✅ 购买数量
    deliveryAddress, // ✅ 配送地址
    deliveryType,    // ✅ 配送方式
    remark          // ✅ 备注
}
```
✅ 字段完整，满足下单需求

#### OrderResponse 字段分析
```java
OrderResponse {
    id, orderNumber, customerId, customerName, customerPhone,
    farmerId, farmerName, farmerPhone,
    productId, productName, productPrice,
    batchId, batchNumber,
    quantity, totalPrice, paymentMethod, orderStatus,
    deliveryAddress, deliveryType,
    logisticsCompany, logisticsNumber,
    orderTime, paymentTime, deliveryTime, receiptTime,
    remark, createTime, updateTime
}
```
✅ 字段完整，满足订单展示需求

---

### ✅ 3. 溯源相关接口（完全满足）

#### 已有接口
```java
GET /api/trace/batch/{batchNumber}         // 查询批次溯源信息 ✅
GET /api/trace/verify/{txHash}             // 验证区块链交易 ✅
```

#### 小程序需求匹配度：100%
- ✅ 扫码溯源（扫描批次编号）
- ✅ 查看批次信息
- ✅ 查看生长记录
- ✅ 区块链验证

#### TraceResponse 字段分析
```java
TraceResponse {
    batchInfo: {
        批次详细信息（种植时间、收获时间、产量等）
    },
    growthRecords: [
        { 生长记录列表（记录时间、生长阶段、描述、图片等） }
    ],
    verification: {
        isVerified,    // 是否已验证
        message,       // 验证消息
        txHash,        // 交易哈希
        chainTime      // 上链时间
    }
}
```
✅ 字段完整，满足溯源展示需求

---

### ⚠️ 4. 用户相关接口（需要补充）

#### 已有接口
```java
POST /api/auth/login                       // 登录 ✅
POST /api/auth/register                    // 注册 ✅
```

#### 小程序需求匹配度：60%

##### ✅ 已满足的功能
1. 用户登录
2. 用户注册

##### ❌ 缺少的功能（重要）
1. **微信授权登录**
   - 缺少：`POST /api/auth/wechat-login` - 微信小程序登录
   - 需要：接收微信 code，调用微信 API 获取 openid
   - 需要：绑定微信用户和系统用户

2. **用户信息接口**
   - 缺少：`GET /api/user/profile` - 获取当前用户信息
   - 缺少：`PUT /api/user/profile` - 更新用户信息

3. **收货地址管理**（重要）
   - 缺少：`GET /api/user/address/list` - 地址列表
   - 缺少：`POST /api/user/address/create` - 添加地址
   - 缺少：`PUT /api/user/address/update/{id}` - 更新地址
   - 缺少：`DELETE /api/user/address/delete/{id}` - 删除地址
   - 缺少：`PUT /api/user/address/set-default/{id}` - 设置默认地址

---

## 二、小程序核心功能分析

### 功能模块 1：商城首页

#### 需要的接口
```javascript
// 1. 获取商品列表（支持分类筛选）
POST /api/product/list
{
  page: 1,
  pageSize: 10,
  category: "蔬菜"  // 可选
}

// 2. 获取商品分类列表
GET /api/product/list-by-category/{category}
```

#### 接口状态：✅ 完全满足

---

### 功能模块 2：商品详情

#### 需要的接口
```javascript
// 1. 获取商品详情
GET /api/product/detail/{id}

// 2. 获取批次溯源信息（可选，用于展示溯源信息）
GET /api/trace/batch/{batchNumber}
```

#### 接口状态：✅ 完全满足

---

### 功能模块 3：购物车（可选）

#### 需要的接口
```javascript
// 购物车功能可以在小程序本地实现（使用 localStorage）
// 不需要后端接口
```

#### 接口状态：✅ 无需后端支持

---

### 功能模块 4：下单购买

#### 需要的接口
```javascript
// 1. 创建订单
POST /api/order/create
{
  customerId: "user123",
  customerName: "张三",
  productId: "prod001",
  batchId: "batch001",
  quantity: 10,
  deliveryAddress: "四川省成都市...",
  deliveryType: "快递配送",
  remark: "请尽快发货"
}

// 2. 发起支付（❌ 缺少）
POST /api/order/pay/{orderId}
{
  paymentMethod: "WECHAT_PAY"
}
// 返回：微信支付参数（prepay_id, sign 等）

// 3. 支付回调（❌ 缺少）
POST /api/order/pay-callback
// 微信支付回调，更新订单状态
```

#### 接口状态：⚠️ 部分满足（缺少支付接口）

---

### 功能模块 5：订单管理

#### 需要的接口
```javascript
// 1. 我的订单列表
POST /api/order/list-by-customer
{
  page: 1,
  pageSize: 10,
  status: 2  // 可选，筛选订单状态
}

// 2. 订单详情
GET /api/order/detail/{id}

// 3. 取消订单（❌ 缺少）
PUT /api/order/cancel/{id}

// 4. 确认收货
PUT /api/order/update-status/{id}?status=4

// 5. 查看物流
GET /api/order/detail/{id}
// 从订单详情中获取物流信息
```

#### 接口状态：⚠️ 部分满足（缺少取消订单接口）

---

### 功能模块 6：溯源查询

#### 需要的接口
```javascript
// 1. 扫码查询溯源信息
GET /api/trace/batch/{batchNumber}

// 2. 验证区块链交易
GET /api/trace/verify/{txHash}
```

#### 接口状态：✅ 完全满足

---

### 功能模块 7：个人中心

#### 需要的接口
```javascript
// 1. 获取用户信息（❌ 缺少）
GET /api/user/profile

// 2. 更新用户信息（❌ 缺少）
PUT /api/user/profile
{
  realName: "张三",
  phone: "13800138000",
  avatar: "https://..."
}

// 3. 收货地址列表（❌ 缺少）
GET /api/user/address/list

// 4. 添加收货地址（❌ 缺少）
POST /api/user/address/create
{
  name: "张三",
  phone: "13800138000",
  province: "四川省",
  city: "成都市",
  district: "高新区",
  detail: "天府大道123号",
  isDefault: true
}

// 5. 更新收货地址（❌ 缺少）
PUT /api/user/address/update/{id}

// 6. 删除收货地址（❌ 缺少）
DELETE /api/user/address/delete/{id}

// 7. 设置默认地址（❌ 缺少）
PUT /api/user/address/set-default/{id}
```

#### 接口状态：❌ 完全缺少

---

## 三、接口完成度总结

### 核心功能完成度

| 功能模块 | 完成度 | 状态 | 说明 |
|---------|--------|------|------|
| 商品展示 | 100% | ✅ | 完全满足 |
| 商品详情 | 100% | ✅ | 完全满足 |
| 溯源查询 | 100% | ✅ | 完全满足 |
| 创建订单 | 100% | ✅ | 完全满足 |
| 订单列表 | 100% | ✅ | 完全满足 |
| 订单详情 | 100% | ✅ | 完全满足 |
| 支付功能 | 0% | ❌ | 完全缺少 |
| 取消订单 | 0% | ❌ | 完全缺少 |
| 用户信息 | 0% | ❌ | 完全缺少 |
| 收货地址 | 0% | ❌ | 完全缺少 |
| 微信登录 | 0% | ❌ | 完全缺少 |

### 总体完成度：60%

---

## 四、需要补充的接口清单

### 🔴 高优先级（必须实现）

#### 1. 支付相关接口
```java
// OrderController.java

// 发起支付
@PostMapping("/pay/{id}")
public WechatPayResponse initiatePayment(@PathVariable String id) {
    // 1. 查询订单信息
    // 2. 调用微信支付统一下单 API
    // 3. 返回支付参数给小程序
}

// 支付回调
@PostMapping("/pay-callback")
public String paymentCallback(@RequestBody String xmlData) {
    // 1. 验证签名
    // 2. 更新订单状态为已支付
    // 3. 返回成功响应给微信
}

// 查询支付状态
@GetMapping("/pay-status/{id}")
public PaymentStatusResponse getPaymentStatus(@PathVariable String id) {
    // 查询订单支付状态
}
```

#### 2. 收货地址管理接口
```java
// AddressController.java

@RestController
@RequestMapping("/api/user/address")
public class AddressController {
    
    // 获取地址列表
    @GetMapping("/list")
    public List<AddressResponse> getAddressList(@RequestParam String userId);
    
    // 添加地址
    @PostMapping("/create")
    public AddressResponse createAddress(@RequestBody AddressRequest request);
    
    // 更新地址
    @PutMapping("/update/{id}")
    public AddressResponse updateAddress(@PathVariable String id, @RequestBody AddressRequest request);
    
    // 删除地址
    @DeleteMapping("/delete/{id}")
    public boolean deleteAddress(@PathVariable String id);
    
    // 设置默认地址
    @PutMapping("/set-default/{id}")
    public boolean setDefaultAddress(@PathVariable String id, @RequestParam String userId);
    
    // 获取默认地址
    @GetMapping("/default")
    public AddressResponse getDefaultAddress(@RequestParam String userId);
}
```

#### 3. 微信登录接口
```java
// AuthController.java

// 微信小程序登录
@PostMapping("/wechat-login")
public LoginResponse wechatLogin(@RequestBody WechatLoginRequest request) {
    // 1. 接收小程序传来的 code
    // 2. 调用微信 API 获取 openid 和 session_key
    // 3. 查询或创建用户
    // 4. 生成 token
    // 5. 返回用户信息和 token
}

// 绑定手机号
@PostMapping("/bind-phone")
public UserResponse bindPhone(@RequestBody BindPhoneRequest request) {
    // 绑定手机号到微信用户
}
```

---

### 🟡 中优先级（建议实现）

#### 4. 订单取消接口
```java
// OrderController.java

@PutMapping("/cancel/{id}")
public OrderResponse cancelOrder(@PathVariable String id, @RequestParam String reason) {
    // 1. 检查订单状态（只能取消待支付订单）
    // 2. 更新订单状态为已取消
    // 3. 如果已支付，发起退款
}
```

#### 5. 用户信息接口
```java
// UserController.java

// 获取当前用户信息
@GetMapping("/profile")
public UserResponse getCurrentUser(@RequestParam String userId);

// 更新用户信息
@PutMapping("/profile")
public UserResponse updateProfile(@RequestBody UserUpdateRequest request);

// 上传头像
@PostMapping("/upload-avatar")
public String uploadAvatar(@RequestParam MultipartFile file);
```

---

### 🟢 低优先级（可选实现）

#### 6. 订单评价接口
```java
// OrderController.java

@PostMapping("/review/{id}")
public ReviewResponse createReview(@PathVariable String id, @RequestBody ReviewRequest request) {
    // 订单评价
}

@GetMapping("/review/{id}")
public ReviewResponse getReview(@PathVariable String id) {
    // 获取订单评价
}
```

#### 7. 收藏功能
```java
// FavoriteController.java

@PostMapping("/favorite/add")
public boolean addFavorite(@RequestParam String userId, @RequestParam String productId);

@DeleteMapping("/favorite/remove")
public boolean removeFavorite(@RequestParam String userId, @RequestParam String productId);

@GetMapping("/favorite/list")
public List<ProductResponse> getFavoriteList(@RequestParam String userId);
```

---

## 五、数据库表补充建议

### 1. 收货地址表（必须）
```sql
CREATE TABLE user_address (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    province VARCHAR(50) NOT NULL,
    city VARCHAR(50) NOT NULL,
    district VARCHAR(50) NOT NULL,
    detail VARCHAR(200) NOT NULL,
    is_default TINYINT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
);
```

### 2. 微信用户绑定表（必须）
```sql
CREATE TABLE wechat_user (
    id VARCHAR(50) PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    openid VARCHAR(100) NOT NULL UNIQUE,
    unionid VARCHAR(100),
    session_key VARCHAR(100),
    nickname VARCHAR(100),
    avatar VARCHAR(200),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_openid (openid)
);
```

### 3. 订单评价表（可选）
```sql
CREATE TABLE order_review (
    id VARCHAR(50) PRIMARY KEY,
    order_id VARCHAR(50) NOT NULL,
    user_id VARCHAR(50) NOT NULL,
    product_id VARCHAR(50) NOT NULL,
    rating INT NOT NULL,
    content TEXT,
    images VARCHAR(500),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_order_id (order_id),
    INDEX idx_product_id (product_id)
);
```

---

## 六、实施建议

### 阶段一：最小可用版本（MVP）- 1周

实现核心购物流程，不包含支付：

1. ✅ 商品展示（已完成）
2. ✅ 商品详情（已完成）
3. ✅ 创建订单（已完成）
4. ✅ 订单列表（已完成）
5. ✅ 溯源查询（已完成）
6. 🔴 收货地址管理（需开发）
7. 🔴 微信登录（需开发）

**说明**：此阶段可以完成小程序的基本流程，订单创建后显示"待支付"，但不能真正支付。

---

### 阶段二：支付功能 - 1周

1. 🔴 集成微信支付 API
2. 🔴 实现支付接口
3. 🔴 实现支付回调
4. 🔴 订单取消和退款

**说明**：此阶段完成后，小程序可以正常下单和支付。

---

### 阶段三：完善功能 - 1周

1. 🟡 用户信息管理
2. 🟡 订单评价
3. 🟢 收藏功能
4. 🟢 优惠券（可选）

---

## 七、结论

### ✅ 可以立即开始开发的功能（60%）

1. 商品展示和详情
2. 溯源查询
3. 订单创建和查询
4. 订单列表展示

### ⚠️ 需要补充接口的功能（40%）

1. 支付功能（必须）
2. 收货地址管理（必须）
3. 微信登录（必须）
4. 订单取消（建议）
5. 用户信息管理（建议）

### 💡 建议

**可以开始开发小程序**，但需要分阶段实施：

1. **第一阶段**：使用现有接口完成商品展示、订单创建、溯源查询等基础功能
2. **第二阶段**：补充收货地址和微信登录接口
3. **第三阶段**：集成微信支付
4. **第四阶段**：完善其他功能

**优势**：
- 现有接口已经覆盖了核心业务逻辑
- 数据结构设计合理，扩展性好
- 溯源功能完整，是产品亮点

**挑战**：
- 需要补充支付相关接口（最重要）
- 需要补充用户地址管理
- 需要实现微信登录授权

**预计开发周期**：3-4周（包含后端接口补充和小程序开发）
