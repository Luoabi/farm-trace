# 西昌农产品溯源系统 - 快速启动指南

## ⚠️ 重要更新（2024年3月）

### ✅ 前端问题已全部修复

前端存在的关键问题已经修复完成：
1. ✅ MainLayout.vue 缺少 nextTick 导入
2. ✅ Store getters 引用错误  
3. ✅ Login.vue dispatch 调用错误
4. ✅ 登录时未传递 remember 参数

**详细信息**：
- 问题分析：`Counter/hotai/前端问题分析报告.md`
- 测试清单：`Counter/hotai/前端修复完成-测试清单.md`

**现在可以正常启动和使用前端应用！**

---

## 🎯 项目概述

这是一个完整的农产品溯源系统，包含：
- **后端**: Spring Boot 3.2.5 + MyBatis + MySQL
- **前端**: Vue 3 + Vite + Element Plus
- **功能**: 用户管理、批次管理、生长记录、商品管理、订单管理

## 📋 前置要求

### 必需软件
- ✅ JDK 17+
- ✅ Maven 3.6+
- ✅ MySQL 8.0+
- ✅ Node.js 16+
- ✅ npm 或 yarn

### 检查安装
```bash
java -version
mvn -version
mysql --version
node -version
npm -version
```

## 🚀 快速启动（5 分钟）

### 第一步：初始化数据库

```bash
# 1. 启动 MySQL 服务
# Windows: 在服务中启动 MySQL
# Mac/Linux: sudo service mysql start

# 2. 执行初始化脚本
mysql -u root -proot < Backend/xichang-interface/src/main/resources/init.sql
```

### 第二步：启动后端

```bash
# 进入后端目录
cd Backend/xichang-interface

# 清理并启动
mvn clean spring-boot:run
```

**看到以下信息说明启动成功：**
```
Started XichangInterfaceApplication in X.XXX seconds
```

### 第三步：启动前端

**打开新的终端窗口：**

```bash
# 进入前端目录
cd Counter/hotai

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

**看到以下信息说明启动成功：**
```
VITE v7.x.x  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 第四步：访问应用

- **前端应用**: http://localhost:5173
- **后端 Swagger**: http://localhost:8080/swagger-ui/index.html
- **API 文档**: http://localhost:8080/v3/api-docs

## 🧪 测试接口

### 方式 1：使用 Swagger UI（推荐）

1. 访问 http://localhost:8080/swagger-ui/index.html
2. 展开 "认证接口"
3. 测试 "POST /api/register" 注册用户
4. 测试 "POST /api/login" 登录

### 方式 2：使用前端测试页面

1. 访问 http://localhost:5173/api-test
2. 点击各个接口测试按钮
3. 查看响应结果

### 方式 3：使用 curl 命令

```bash
# 测试注册
curl -X POST http://localhost:8080/api/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testfarmer\",\"password\":\"123456\",\"phone\":\"13800138000\",\"email\":\"test@example.com\",\"userType\":\"FARMER\",\"organization\":\"测试农场\",\"farmName\":\"测试农场\",\"farmAddress\":\"四川省西昌市\"}"

# 测试登录
curl -X POST http://localhost:8080/api/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testfarmer\",\"password\":\"123456\"}"
```

## 📁 项目结构

```
.
├── Backend/xichang-interface/          # 后端项目
│   ├── src/main/java/                  # Java 源码
│   │   └── org/xingchang/xichanginterface/
│   │       ├── controller/             # 控制器
│   │       ├── service/                # 服务层
│   │       ├── mapper/                 # 数据访问层
│   │       ├── model/                  # 实体类
│   │       ├── dto/                    # 数据传输对象
│   │       └── config/                 # 配置类
│   ├── src/main/resources/             # 资源文件
│   │   ├── application.properties      # 配置文件
│   │   ├── init.sql                    # 数据库初始化脚本
│   │   └── mapper/                     # MyBatis XML
│   ├── pom.xml                         # Maven 配置
│   ├── 测试指南.md                      # 测试文档
│   └── postman_collection.json         # Postman 测试集合
│
└── Counter/hotai/                      # 前端项目
    ├── src/
    │   ├── api/                        # API 接口
    │   │   ├── modules/                # API 模块
    │   │   │   ├── user.js             # 用户接口
    │   │   │   ├── batch.js            # 批次接口
    │   │   │   ├── growthRecord.js     # 生长记录接口
    │   │   │   ├── product.js          # 商品接口
    │   │   │   └── order.js            # 订单接口
    │   │   ├── request.js              # Axios 配置
    │   │   └── index.js                # API 统一导出
    │   ├── views/                      # 页面组件
    │   ├── components/                 # 公共组件
    │   ├── router/                     # 路由配置
    │   ├── store/                      # Vuex 状态管理
    │   └── main.js                     # 入口文件
    ├── vite.config.js                  # Vite 配置
    ├── package.json                    # 依赖配置
    ├── 前端API使用文档.md               # API 使用文档
    └── 前端API完善总结.md               # API 完善总结
```

## 🔑 测试账号

数据库初始化后会自动创建以下测试数据：

### 商品数据
- `product-001`: 西昌葡萄（58元/kg）
- `product-002`: 西昌苹果（38元/kg）
- `product-003`: 西昌萝卜（8元/kg）

### 用户数据（需要通过注册接口创建）
```json
{
  "username": "testfarmer",
  "password": "123456",
  "phone": "13800138000",
  "email": "test@example.com",
  "userType": "FARMER",
  "organization": "测试农场",
  "farmName": "测试农场",
  "farmAddress": "四川省西昌市"
}
```

## 📝 开发文档

### 后端文档
- `Backend/xichang-interface/README.md` - 项目说明
- `Backend/xichang-interface/API接口文档.md` - 接口文档
- `Backend/xichang-interface/测试指南.md` - 测试指南

### 前端文档
- `Counter/hotai/前端API使用文档.md` - API 使用文档
- `Counter/hotai/前端API完善总结.md` - API 完善总结
- `Counter/hotai/界面功能和接口返回格式文档.md` - 界面功能文档

## ⚠️ 常见问题

### 1. 数据库连接失败
**错误**: `Communications link failure`

**解决**:
- 检查 MySQL 服务是否启动
- 确认用户名密码（默认 root/root）
- 检查端口是否为 3306

### 2. 端口被占用
**错误**: `Port 8080 was already in use`

**解决**:
```bash
# 查找占用进程
netstat -ano | findstr :8080

# 结束进程
taskkill /F /PID <进程ID>
```

### 3. Maven 依赖下载慢
**解决**: 配置阿里云镜像
编辑 `~/.m2/settings.xml`:
```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>central</mirrorOf>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### 4. npm 安装依赖慢
**解决**: 使用淘宝镜像
```bash
npm config set registry https://registry.npmmirror.com
```

### 5. Swagger 报 500 错误
**解决**: 已修复版本冲突，重新启动后端即可

### 6. 前端跨域问题
**解决**: 已在 `vite.config.js` 中配置代理，无需额外处理

## 🎯 下一步

1. **熟悉项目结构**
   - 查看后端 Controller 和 Service
   - 查看前端 API 模块和组件

2. **测试所有接口**
   - 使用 Swagger UI 测试后端接口
   - 使用前端测试页面验证集成

3. **开始开发**
   - 根据需求添加新功能
   - 完善现有业务逻辑

4. **部署上线**
   - 配置生产环境数据库
   - 打包前后端应用
   - 部署到服务器

## 📞 技术支持

遇到问题？
1. 查看相关文档
2. 检查浏览器控制台和后端日志
3. 使用 Swagger UI 测试接口
4. 查看 GitHub Issues

祝开发顺利！🎉
